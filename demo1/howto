# What for
  
  Get inside information in auto-sklearn, of "automl",
  
  which a result of autosklearn.classification.AutoSklearnClassifier (as you can see in model.py)


# Dependency
  pip3 install flask_bootstrap

# Run
  python3 model.py
  
# Visit
  http://127.0.0.1/models    (or Your Server IP)
